{
  "name": "wish-course",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Client pre-built — skipping'",
    "build:local": "cd client && npm install && npm run build",
    "start": "node server/index.js",
    "install:server": "cd server && npm install"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "pg": "^8.13.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
